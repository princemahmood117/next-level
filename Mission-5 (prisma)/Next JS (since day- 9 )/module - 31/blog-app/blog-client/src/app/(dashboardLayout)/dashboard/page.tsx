
const DashboardPage = ({children} : {children : React.ReactNode}) => {
    return (
        <div>
            this is dashboard
            {children}
        </div>
    );
};

export default DashboardPage;